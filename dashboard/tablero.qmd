---
title: "Formal vs Informal - CDMX"
subtitle: "Análisis de la brecha de ingresos en la Ciudad de México"
format:
  dashboard:
    orientation: rows
    scrolling: true
---


```{python}
import pandas as pd
import plotly.express as px
import plotly.graph_objects as go
import plotly.io as pio
pio.renderers.default = "notebook"
# Cargar datos
df = pd.read_csv('../datos/enoe_cdmx_limpio.csv')


# Resumen por sector
resumen = df.groupby('sector').agg(
    horas=('horas_semana', 'mean'),
    ingreso=('ingreso_mensual', 'mean'),
    pago_hora=('ingreso_por_hora', 'mean'),
    conteo=('sector', 'count')
).reset_index().round(1)


# Calcular diferencia
ingreso_formal = resumen[resumen['sector'] == 'Formal']['ingreso'].values[0]
ingreso_informal = resumen[resumen['sector'] == 'Informal']['ingreso'].values[0]
diferencia = ingreso_formal - ingreso_informal
porcentaje_diferencia = ((ingreso_formal - ingreso_informal) / ingreso_formal * 100)
```


# Sector


## Row


```{python}
#| content: valuebox
#| title: "Ingreso Formal"
dict(
    icon="briefcase",
    color="success",
    value=f"${ingreso_formal:,.0f}"
)
```


```{python}
#| content: valuebox
#| title: "Ingreso Informal"
dict(
    icon="exclamation-triangle",
    color="danger",
    value=f"${ingreso_informal:,.0f}"
)
```


```{python}
#| content: valuebox
#| title: "Diferencia"
dict(
    icon="cash-coin",
    color="warning",
    value=f"${diferencia:,.0f}"
)
```


```{python}
#| content: valuebox
#| title: "Brecha"
dict(
    icon="percent",
    color="info",
    value=f"{porcentaje_diferencia:.0f}%"
)
```
## Row {height="10%"}
```{python}
#| title: "Ingreso mensual promedio (MXN)"
fig1 = px.bar(
    resumen, x='sector', y='ingreso',
    color='sector',
    color_discrete_map={'Formal': '#14B8A6', 'Informal': '#F59E0B'},
    text='ingreso'
)
# ← texttemplate VA AQUÍ, en traces:
fig1.update_traces(
    texttemplate='$%{text:,.0f}',
    textposition='outside'
)
# ← height y ejes VAN AQUÍ, en layout:
fig1.update_layout(
    showlegend=False,
    yaxis_title='Ingreso (MXN)',
    xaxis_title='',
    yaxis_tickprefix='$',
    yaxis_tickformat=',',
    height=350  # ← más pequeño
)
```

## Row {height="10%"}
```{python}
#| title: "Distribución de trabajadores"
fig2 = px.bar(
    resumen, x='sector', y='conteo',
    color='sector',
    color_discrete_map={'Formal': '#14B8A6', 'Informal': '#F59E0B'},
    text='conteo'
)
fig2.update_traces(
    texttemplate='%{text:,.0f}',
    textposition='outside'
)
fig2.update_layout(
    showlegend=False,
    yaxis_title='Cantidad',
    xaxis_title='',
    height=350
)
```


## Row {height="10%"}
```{python}
#| title: "Horas trabajadas vs Ingreso por hora"
fig3 = px.scatter(
    df,
    x='horas_semana', y='ingreso_por_hora',
    color='sector',
    color_discrete_map={'Formal': '#14B8A6', 'Informal': '#F59E0B'},
    opacity=0.5,
    trendline='ols'
)
fig3.update_layout(
    xaxis_title='Horas por semana',
    yaxis_title='Ingreso por hora (MXN)',
    showlegend=True,
    height=450
)


```


# Contexto


La informalidad en CDMX reduce el ingreso promedio casi a la mitad. Esto afecta directamente la capacidad de ahorro, acceso a servicios y calidad de vida de millones de personas.

# ocupacion
## Row {height="10%"}
```{python}
#| title: "Horas semanales de traslado al trabajo por sexo"
act = pd.read_csv('../datos/tvar_crea.csv', low_memory=False)
cdmx = act[act['CVE_ENT'] == 9].copy()
cdmx['sexo'] = cdmx['SEXO'].map({1: 'Hombre', 2: 'Mujer'})
traslado_sexo = cdmx.groupby('sexo')['TRAS_TRAB'].mean().reset_index()
traslado_sexo.columns = ['Sexo', 'Horas_traslado']
traslado_sexo['Horas_traslado'] = traslado_sexo['Horas_traslado'].round(2)

fig4 = px.bar(
    traslado_sexo,
    x='Sexo', y='Horas_traslado',
    color='Sexo',
    color_discrete_map={'Hombre': '#3B82F6', 'Mujer': '#EC4899'},
    text='Horas_traslado'
)
fig4.update_traces(
    texttemplate='%{text} hrs', 
    textposition='outside'
)
fig4.update_layout(
    showlegend=False, 
    yaxis_title='Horas por semana', 
    xaxis_title='', 
    height=250
)
```

## Row {height="15%"}
```{python}
#| title: "Tiempo libre semanal por condición de actividad"
act = pd.read_csv('../datos/tvar_crea.csv', low_memory=False)
cdmx = act[act['CVE_ENT'] == 9].copy()
cdmx['condicion'] = cdmx['COND_AEE'].map({
    1: 'Ocupada/o',
    2: 'Con vínculo laboral', 
    3: 'Desocupada/o',
    4: 'Estudiante',
    5: 'Quehaceres del hogar',
    6: 'Otro inactivo'
})

tlibre = cdmx.groupby('condicion')['ACTIV_CONVIV'].mean().reset_index()
tlibre.columns = ['Condicion', 'Tiempo_libre']
tlibre = tlibre.sort_values('Tiempo_libre')

fig5 = px.bar(
    tlibre,
    x='Tiempo_libre', y='Condicion',
    orientation='h',
    color='Tiempo_libre',
    color_continuous_scale='Teal',
    text='Tiempo_libre'
)
fig5.update_traces(
    texttemplate='%{text:.1f} hrs', 
    textposition='outside'
)
fig5.update_layout(
    coloraxis_showscale=False, 
    xaxis_title='Horas de convivencia/semana', 
    yaxis_title='', 
    height=300
)
```

## Row {height="15%"}
```{python}
#| title: "Resumen tiempo por condición laboral"
act = pd.read_csv('../datos/tvar_crea.csv', low_memory=False)
cdmx = act[act['CVE_ENT'] == 9].copy()
cdmx['condicion'] = cdmx['COND_AEE'].map({
    1: 'Ocupada/o',
    2: 'Con vínculo laboral', 
    3: 'Desocupada/o',
    4: 'Estudiante',
    5: 'Quehaceres del hogar',
    6: 'Otro inactivo'
})

resumen = cdmx.groupby('condicion').agg(
    traslado=('TRAS_TRAB', 'mean'),
    trabajo=('ACTIV_MERC', 'mean'),
    tiempo_libre=('ACTIV_CONVIV', 'mean'),
    n=('COND_AEE', 'count')
).round(2).reset_index()

fig6 = px.bar(
    resumen,
    x='condicion', y=['traslado', 'trabajo', 'tiempo_libre'],
    title="Tiempos semanales por condición (hrs)",
    color_discrete_map={
        'traslado': '#F59E0B',
        'trabajo': '#14B8A6', 
        'tiempo_libre': '#8B5CF6'
    }
)
fig6.update_layout(
    showlegend=True,
    xaxis_title='Condición laboral',
    yaxis_title='Horas por semana',
    height=300
)
```