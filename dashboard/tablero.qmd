---
title: "Formal vs Informal - CDMX"
subtitle: "Análisis de la brecha de ingresos en la Ciudad de México"
format:
  dashboard:
    orientation: rows
    scrolling: true
---

```{python}
#| output: false
import pandas as pd
import plotly.express as px
import plotly.graph_objects as go

df = pd.read_csv('../datos/enoe_cdmx_limpio.csv')

resumen = df.groupby('sector').agg(
    horas=('horas_semana', 'mean'),
    ingreso=('ingreso_mensual', 'mean'),
    pago_hora=('ingreso_por_hora', 'mean'),
    conteo=('sector', 'count')
).reset_index().round(1)

ingreso_formal    = resumen[resumen['sector'] == 'Formal']['ingreso'].values[0]
ingreso_informal  = resumen[resumen['sector'] == 'Informal']['ingreso'].values[0]
diferencia        = ingreso_formal - ingreso_informal
porcentaje_diferencia = (diferencia / ingreso_formal * 100)
```

# Sector

## Row

```{python}
#| content: valuebox
#| title: "Ingreso Formal"
dict(icon="briefcase", color="success", value=f"${ingreso_formal:,.0f}")
```

```{python}
#| content: valuebox
#| title: "Ingreso Informal"
dict(icon="exclamation-triangle", color="danger", value=f"${ingreso_informal:,.0f}")
```

```{python}
#| content: valuebox
#| title: "Diferencia"
dict(icon="cash-coin", color="warning", value=f"${diferencia:,.0f}")
```

```{python}
#| content: valuebox
#| title: "Brecha"
dict(icon="percent", color="info", value=f"{porcentaje_diferencia:.0f}%")
```

## Row {height="35%"}

```{python}
#| title: "Ingreso mensual promedio (MXN)"
fig1 = px.bar(
    resumen, x='sector', y='ingreso',
    color='sector',
    color_discrete_map={'Formal': '#14B8A6', 'Informal': '#F59E0B'},
    text='ingreso'
).update_traces(
    texttemplate='$%{text:,.0f}', textposition='outside'
).update_layout(
    showlegend=False,
    yaxis_title='Ingreso (MXN)',
    xaxis_title='',
    yaxis_tickprefix='$',
    yaxis_tickformat=',',
    height=350
)
fig1.show()
```



```{python}
#| title: "Distribución de trabajadores"
fig2 = px.bar(
    resumen, x='sector', y='conteo',
    color='sector',
    color_discrete_map={'Formal': '#14B8A6', 'Informal': '#F59E0B'},
    text='conteo'
).update_traces(
    texttemplate='%{text:,.0f}', textposition='outside'
).update_layout(
    showlegend=False,
    yaxis_title='Cantidad',
    xaxis_title='',
    height=350
)
fig2.show()
```

## Row {height="40%"}

```{python}
#| title: "Horas trabajadas vs Ingreso por hora"
fig3 = px.scatter(
    df,
    x='horas_semana', y='ingreso_por_hora',
    color='sector',
    color_discrete_map={'Formal': '#14B8A6', 'Informal': '#F59E0B'},
    opacity=0.5,
    trendline='ols'
).update_layout(
    xaxis_title='Horas por semana',
    yaxis_title='Ingreso por hora (MXN)',
    showlegend=True,
    height=450
)
fig3.show()
```

# Ocupacion y genero

## Row

```{python}
#| output: false
act  = pd.read_csv('../datos/tvar_crea.csv', low_memory=False)
cdmx = act[act['CVE_ENT'] == 9].copy()
cdmx['sexo'] = cdmx['SEXO'].map({1: 'Hombre', 2: 'Mujer'})
cdmx['condicion'] = cdmx['COND_AEE'].map({
    1: 'Ocupada/o',
    2: 'Con vínculo laboral',
    3: 'Desocupada/o',
    4: 'Estudiante',
    5: 'Quehaceres del hogar',
    6: 'Otro inactivo'
})

# Calcular métricas de género y ocupación
total_mujeres = len(cdmx[cdmx['sexo'] == 'Mujer'])
total_hombres = len(cdmx[cdmx['sexo'] == 'Hombre'])

mujeres_ocupadas = len(cdmx[(cdmx['sexo'] == 'Mujer') & (cdmx['condicion'] == 'Ocupada/o')])
hombres_ocupados = len(cdmx[(cdmx['sexo'] == 'Hombre') & (cdmx['condicion'] == 'Ocupada/o')])

pct_mujeres_ocupadas = (mujeres_ocupadas / total_mujeres * 100) if total_mujeres > 0 else 0
pct_hombres_ocupados = (hombres_ocupados / total_hombres * 100) if total_hombres > 0 else 0

brecha_participacion = pct_hombres_ocupados - pct_mujeres_ocupadas

# Horas de trabajo
horas_mujeres = cdmx[cdmx['sexo'] == 'Mujer']['ACTIV_MERC'].mean() if total_mujeres > 0 else 0
horas_hombres = cdmx[cdmx['sexo'] == 'Hombre']['ACTIV_MERC'].mean() if total_hombres > 0 else 0
diferencia_horas = horas_hombres - horas_mujeres
```

```{python}
#| content: valuebox
#| title: "% Mujeres Ocupadas"
dict(icon="person-check", color="danger", value=f"{pct_mujeres_ocupadas:.0f}%")
```

```{python}
#| content: valuebox
#| title: "% Hombres Ocupados"
dict(icon="person-check", color="info", value=f"{pct_hombres_ocupados:.0f}%")
```

```{python}
#| content: valuebox
#| title: "Brecha de Participación"
dict(icon="arrow-left-right", color="warning", value=f"{brecha_participacion:.1f}pp")
```

```{python}
#| content: valuebox
#| title: "Diferencia Horas de Trabajo"
dict(icon="clock", color="success", value=f"{diferencia_horas:.1f} hrs")
```

## Row {height="30%"}

```{python}
#| title: "Horas semanales de traslado al trabajo por sexo"
traslado_sexo = cdmx.groupby('sexo')['TRAS_TRAB'].mean().reset_index()
traslado_sexo.columns = ['Sexo', 'Horas_traslado']
traslado_sexo['Horas_traslado'] = traslado_sexo['Horas_traslado'].round(2)

fig4 = px.bar(
    traslado_sexo,
    x='Sexo', y='Horas_traslado',
    color='Sexo',
    color_discrete_map={'Hombre': '#3B82F6', 'Mujer': '#EC4899'},
    text='Horas_traslado'
).update_traces(
    texttemplate='%{text} hrs', textposition='outside'
).update_layout(
    showlegend=False,
    yaxis_title='Horas por semana',
    xaxis_title='',
    height=250
)
fig4.show()
```



```{python}
#| title: "Tiempo libre semanal por condición de actividad"
tlibre = cdmx.groupby('condicion')['ACTIV_CONVIV'].mean().reset_index()
tlibre.columns = ['Condicion', 'Tiempo_libre']
tlibre = tlibre.sort_values('Tiempo_libre')

fig5 = px.bar(
    tlibre,
    x='Tiempo_libre', y='Condicion',
    orientation='h',
    color='Tiempo_libre',
    color_continuous_scale='Teal',
    text='Tiempo_libre'
).update_traces(
    texttemplate='%{text:.1f} hrs', textposition='outside'
).update_layout(
    coloraxis_showscale=False,
    xaxis_title='Horas de convivencia/semana',
    yaxis_title='',
    height=300
)
fig5.show()
```

## Row {height="35%"}

```{python}
#| title: "Resumen tiempo por condición laboral"
resumen_enut = cdmx.groupby('condicion').agg(
    traslado=('TRAS_TRAB', 'mean'),
    trabajo=('ACTIV_MERC', 'mean'),
    tiempo_libre=('ACTIV_CONVIV', 'mean'),
    n=('COND_AEE', 'count')
).round(2).reset_index()

fig6 = px.bar(
    resumen_enut,
    x='condicion', y=['traslado', 'trabajo', 'tiempo_libre'],
    color_discrete_map={
        'traslado': '#F59E0B',
        'trabajo': '#14B8A6',
        'tiempo_libre': '#8B5CF6'
    }
).update_layout(
    showlegend=True,
    xaxis_title='Condición laboral',
    yaxis_title='Horas por semana',
    height=300
)
fig6.show()
```

# Metro

## Row 

```{python}
#| output: false
ZONA_NORTE = ["Gustavo A. Madero", "Azcapotzalco", "Venustiano Carranza", "Estado de México", "Nezahualcóyotl (Edomex)"]
ZONA_SUR   = ["Coyoacán", "Álvaro Obregón", "Magdalena Contreras", "Tláhuac", "Xochimilco"]

df_metro = pd.read_csv('../datos/metro_limpio.csv', parse_dates=["fecha"])
df_metro["zona"] = df_metro["alcaldia"].apply(
    lambda a: "Norte/Nororiente" if a in ZONA_NORTE else ("Sur/Poniente" if a in ZONA_SUR else "Centro")
)
df_metro["dia_semana"] = df_metro["fecha"].dt.dayofweek
df_metro["dia_nombre"] = df_metro["dia_semana"].map({0:"Lun",1:"Mar",2:"Mié",3:"Jue",4:"Vie",5:"Sáb",6:"Dom"})

# Calcular métricas del Metro
total_viajes = df_metro["afluencia"].sum()
total_viajes_millones = total_viajes / 1_000_000

afluencia_promedio_diaria = df_metro.groupby("fecha")["afluencia"].sum().mean()
afluencia_promedio_millones = afluencia_promedio_diaria / 1_000_000

# Zona con más afluencia
afluencia_por_zona = df_metro.groupby("zona")["afluencia"].sum()
zona_mayor = afluencia_por_zona.idxmax()

# Año con mayor afluencia
anio_mayor = df_metro['fecha'].dt.year.max()
afluencia_anio_mayor = df_metro[df_metro['fecha'].dt.year == anio_mayor]["afluencia"].sum() / 1_000_000
```

```{python}
#| content: valuebox
#| title: "Viajes Totales"
dict(icon="train-subway", color="info", value=f"{total_viajes_millones:.1f}M")
```

```{python}
#| content: valuebox
#| title: "Afluencia Promedio Diaria"
dict(icon="graph-up", color="success", value=f"{afluencia_promedio_millones:.2f}M")
```



```{python}
#| content: valuebox
#| title: "Total de Estaciones"
dict(icon="building", color="danger", value=f"195")
```

## Row {height="30%"}




```{python}
#| title: "Mapa de Estaciones Críticas (Afluencia Acumulada)"
from plotly.subplots import make_subplots

def crear_mapa():
    top_est = df_metro.groupby(["estacion", "alcaldia"])["afluencia"].sum().reset_index()
    top_est = top_est.sort_values("afluencia", ascending=False).head(15)
    top_est["afluencia_millones"] = (top_est["afluencia"] / 1_000_000).round(2)

    coords = {
        'Indios Verdes': [19.4954, -99.1195], 'Cuatro Caminos': [19.4596, -99.2158],
        'Constitución de 1917': [19.3460, -99.0639], 'Tasqueña': [19.3437, -99.1395],
        'Pantitlán': [19.4151, -99.0743], 'Universidad': [19.3243, -99.1739],
        'Zócalo/Tenochtitlan': [19.4325, -99.1322], 'Observatorio': [19.3982, -99.2004],
        'Ciudad Azteca': [19.5341, -99.0272], 'Buenavista': [19.4468, -99.1531],
        'Tacubaya': [19.4032, -99.1871], 'Chapultepec': [19.4204, -99.1763],
        'Insurgentes': [19.4233, -99.1628], 'Zaragoza': [19.4121, -99.0823],
        'Merced': [19.4255, -99.1245]
    }

    top_est['lat'] = top_est['estacion'].map(lambda x: coords.get(x, [None, None])[0])
    top_est['lon'] = top_est['estacion'].map(lambda x: coords.get(x, [None, None])[1])
    df_mapa = top_est.dropna(subset=['lat']).copy()

    colores = ['#E63946', '#457B9D', '#1D3557', '#A8DADC', '#F1FAEE', '#FFB703', '#FB8500', '#8E9AAF', '#CBC0D3', '#EFD3D7']
    df_mapa["color"] = [colores[i % len(colores)] for i in range(len(df_mapa))]
    marker_sizes = (df_mapa['afluencia_millones'] * 6).clip(lower=16, upper=44)

    fig = make_subplots(
        rows=1, cols=2,
        specs=[[{"type": "scattermapbox"}, {"type": "table"}]],
        column_widths=[0.62, 0.38],
        horizontal_spacing=0.03
    )

    fig.add_trace(go.Densitymapbox(
        lat=df_mapa['lat'],
        lon=df_mapa['lon'],
        z=df_mapa['afluencia_millones'],
        radius=38,
        opacity=0.55,
        colorscale='YlOrRd',
        hoverinfo='skip',
        showscale=False
    ), row=1, col=1)

    fig.add_trace(go.Scattermapbox(
        lat=df_mapa['lat'], lon=df_mapa['lon'],
        mode='markers',
        marker=dict(size=marker_sizes, sizemode='diameter', sizemin=12, color=df_mapa['color'], opacity=0.8),
        customdata=df_mapa[['alcaldia', 'afluencia_millones']],
        text=df_mapa['estacion'],
        hovertemplate='<b>%{text}</b><br>Alcaldía: %{customdata[0]}<br>Afluencia: %{customdata[1]:.2f}M<extra></extra>',
        showlegend=False
    ), row=1, col=1)

    fig.add_trace(go.Table(
        header=dict(values=['', 'Estación', 'Alcaldía', 'Afluencia'],
            fill_color='#F3F4F6', align=['center', 'left', 'left', 'right'],
            font=dict(color='#111827', size=12), height=28),
        cells=dict(
            values=[[''] * len(df_mapa), df_mapa['estacion'], df_mapa['alcaldia'],
                    [f"{v:.2f}M" for v in df_mapa['afluencia_millones']]],
            fill_color=[df_mapa['color'], ['white']*len(df_mapa), ['white']*len(df_mapa), ['white']*len(df_mapa)],
            align=['center', 'left', 'left', 'right'],
            font=dict(color=['rgba(0,0,0,0)', '#111827', '#111827', '#111827'], size=11),
            height=24),
        columnwidth=[26, 140, 120, 70]
    ), row=1, col=2)

    fig.update_layout(
        height=460, margin=dict(l=0, r=0, t=10, b=0),
        mapbox=dict(style='carto-positron', zoom=10.3, center=dict(lat=19.4326, lon=-99.1332))
    )
    return fig

crear_mapa()
```

## Row {height="30%"}

```{python}
#| title: "Tendencia Norte vs Sur (2010-2025)"
tendencia = df_metro.groupby(["anio","zona"])["afluencia"].sum().reset_index()

px.line(
    tendencia,
    x='anio', y='afluencia',
    color='zona',
    color_discrete_map={'Norte/Nororiente': '#F59E0B', 'Sur/Poniente': '#14B8A6', 'Centro': '#8B5CF6'},
    markers=True,
    title=""
)

```

## Row {height="30%"}

```{python}
#| title: "Patrón semanal - Zona Norte/Nororiente"
norte_semanal = df_metro[df_metro["zona"] == "Norte/Nororiente"].groupby("dia_nombre")["afluencia"].mean().reset_index()
norte_semanal["afluencia_miles"] = (norte_semanal["afluencia"] / 1000).round(0)

orden_dias = ["Lun","Mar","Mié","Jue","Vie","Sáb","Dom"]
norte_semanal["dia_nombre"] = pd.Categorical(norte_semanal["dia_nombre"], categories=orden_dias, ordered=True)
norte_semanal = norte_semanal.sort_values("dia_nombre")

fig10 = px.bar(
    norte_semanal,
    x='dia_nombre', y='afluencia_miles',
    text='afluencia_miles'
).update_traces(
    texttemplate='%{text:.0f}K', textposition='outside'
).update_layout(
    showlegend=False,
    xaxis_title='Día de la semana',
    yaxis_title='Miles de viajes',
    height=280
)
fig10.show()
```
